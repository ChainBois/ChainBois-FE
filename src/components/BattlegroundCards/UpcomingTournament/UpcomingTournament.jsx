'use client'

import s from "@/styles"
import { cf } from "@/utils"
import a from "./UpcomingTournament.module.css"

export default function UpcomingTournament({}) {
	return (
		<section className={cf(s.wMax, s.flex, s.spaceXCenter, a.tournamentCard)}>
			
		</section>
	)
}
